FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir flask gunicorn requests python-dotenv
COPY . .
EXPOSE 5000
CMD ["gunicorn", "landing.server:app", "--bind", "0.0.0.0:5000", "--workers", "2"]
